def bold(text: str) -> str:
    """
    Wraps the given text in HTML bold tags.
    
    Args:
        text (str): The text to be wrapped.
    
    Returns:
        str: The text wrapped in <b> HTML tags.
    """
    return f"<b>{text}</b>"