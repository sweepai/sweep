import { useState, useEffect, useRef } from "react"
import { RepoPreview } from "../../components/RepoPreview"
import { PRPreview } from "../../components/PRPreview"

# 🎨 Example Issues Solved by Sweep

For Sweep's resume, see [here](/resume.pdf).

<RepoPreview repoName="sweepai/sweep" />

We use Sweep internally to fix small issues such as fixing a path to support windows and bug fixes like Python’s calling of git pull failing. We also have been doing setup things like GitHub Actions and ticket templates.

<PRPreview repoName="sweepai/sweep" prId={2492}/>
<PRPreview repoName="sweepai/sweep" prId={2470}/>
<PRPreview repoName="sweepai/sweep" prId={2464}/>

<RepoPreview repoName="sweepai/landing-page" displayName="Sweep Landing Page" />

We use Sweep for our landing page. We use Netlify to check for build completion and previews of our landing page and do iterations against the Netlify previews. This makes modifying the landing page possible with non-frontend devs like Will.

<PRPreview repoName="sweepai/landing-page" prId={485}/>
<PRPreview repoName="sweepai/landing-page" prId={450}/>
<PRPreview repoName="sweepai/landing-page" prId={319}/>
<PRPreview repoName="sweepai/landing-page" prId={18}/>
<PRPreview repoName="sweepai/landing-page" prId={138}/>

<RepoPreview repoName="medplum/medplum" />

Medplum uses Sweep for small refactors and bug fixes.

<PRPreview repoName="medplum/medplum" prId={2365}/>
<PRPreview repoName="medplum/medplum" prId={2378}/>
<PRPreview repoName="medplum/medplum" prId={2412}/>
<PRPreview repoName="medplum/medplum" prId={2411}/>

<RepoPreview repoName="linksplatform/Data" />

LinksPlatform/Data uses Sweep for small code changes.

<PRPreview repoName="linksplatform/Data" prId={137}/>

<RepoPreview repoName="linksplatform/Interfaces" />

LinksPlatform/Interfaces uses Sweep for small code changes.

<PRPreview repoName="linksplatform/Interfaces" prId={116}/>

<RepoPreview repoName="gradio-app/gradio" />

<PRPreview repoName="gradio-app/gradio" prId={5104}/>

<RepoPreview repoName="pytorch/ignite" displayName="PyTorch Ignite" />

PyTorch Ignite uses Sweep for documentation fixes.

<PRPreview repoName="pytorch/ignite" prId={2984}/>
<PRPreview repoName="pytorch/ignite" prId={2989}/>

---

To contribute more, feel free to ping Kevin and Will on Discourse.
