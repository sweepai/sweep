def a_func():
    a = 1
    b = 2 * a
    c = a * 2 + b * 3
    logging.debug(f"{b}, {c}")def a_func():
    a = 1
    b = 2 * a
    c = a * 2 + b * 3
    logging.debug(f"{b}, {c}")
import logging
=======
import logging
