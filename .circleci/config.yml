# This config was automatically generated from your source code
# Stacks detected: cicd:github-actions:.github/workflows,deps:node:platform,deps:python:.,package_manager:poetry:.,test:jest:
version: 2.1
orbs:
  python: circleci/python@2
jobs:
  test-job:
    docker:
      - image: cimg/python:3.10-node
    steps:
      - checkout
      - python/install-packages:
          pkg-manager: poetry
      - run:
          name: ls
          command: ls
      - run:
          name: Run tests
          command: python sweepai/sweep/test/e2e/test_sweep.py
      - store_test_results:
          path: junit.xml

workflows:
  build-and-test:
    jobs:
      - test-job
