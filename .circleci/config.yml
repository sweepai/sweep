# This config was automatically generated from your source code
# Stacks detected: cicd:github-actions:.github/workflows,deps:node:platform,deps:python:.,package_manager:poetry:.,test:jest:
version: 2.1
orbs:
  python: circleci/python@2
jobs:
  test-job:
    docker:
      - image: cimg/python:3.10-node
    steps:
      - checkout
      - python/install-packages:
          pkg-manager: poetry
      - run:
          name: ls
          command: ls
      - run:
          name: env vars
          command: echo ${GITHUB_PAT}
      - run:
          name: create env var
          command: |
            echo "GITHUB_PAT=${GITHUB_PAT}" >> .env
            echo "GITHUB_APP_ID=${GITHUB_APP_ID}" >> .env
            echo "OPENAI_API_KEY=${OPENAI_API_KEY}" >> .env
      - run:
          name: Run tests
          command: poetry run python tests/e2e/test_sweep_issue.py
      - store_test_results:
          path: junit.xml

workflows:
  build-and-test:
    jobs:
      - test-job
